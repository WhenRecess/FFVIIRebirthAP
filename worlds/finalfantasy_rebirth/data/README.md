# Data Directory

Place extracted game data CSV files here:

- `items.csv`: Item definitions extracted from game assets
- `territories.csv`: Territory/location definitions
- `enemies.csv`: Enemy template definitions (optional)

See `README_WORLD.md` for CSV format specifications and `tools/exporter_example.cs` for extraction examples.
